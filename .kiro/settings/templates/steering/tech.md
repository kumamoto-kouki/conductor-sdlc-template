# tech.md

<!--
Bootstrap template for /kiro-steering (kiro-steering skill, Bootstrap Flow
Step 1). Content is generated from codebase analysis (configs, dependencies,
frameworks), then reviewed by the user/orchestrator -- write in the project's
documentation language. Follow rules/steering-principles.md: key frameworks
and conventions, not a full dependency list.
-->

## Stack

- Language / runtime: <e.g.>
- Framework / key libraries: <e.g.>
- Testing: <e.g. unit / integration / E2E tooling>
- Build / type-check: <e.g.>

## Commands (verification source of truth)

- Test: `<command>`
- Type-check: `<command>`
- Build: `<command>`
- E2E: `<command>`

<!-- Task/implementation acceptance is based on the fresh output of these
commands (see kiro-verify-completion), not on restated claims. -->

## Constraints & Conventions

- Security: <e.g. secret handling, allowed egress>
- Performance: <e.g. bundle size, startup budget>
- Dependency policy: <e.g. bias against adding new dependencies>

## Stack-Specific Decision Criteria

<!-- Promote a decision here only after it has been made twice the same way
during implementation; see .claude/rules/README.md for the same threshold
applied to path-scoped rule files. -->
