{
  "name": "conductor-sdlc-project",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "conductor-sdlc-template から生成したプロジェクト。`npm run status` が STATUS.md（工程・承認・タスク消化・配役の導出結果）を再生成し、`npm run verify` が文書とテンプレート構成の整合を検証する。テンプレート由来の依存パッケージは無い（このプロジェクト自身の依存は必要になった時点で追加する）。",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "status": "node scripts/status-report.mjs",
    "verify": "node scripts/verify.mjs"
  },
  "devDependencies": {}
}
