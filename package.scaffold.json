{
  "name": "conductor-sdlc-template-dashboard",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Astro ベースの開発ダッシュボード（dashboard/status.json を唯一の真実としてビルド時に静的HTML化する）",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/preclean-astro-output.mjs && astro build && node scripts/relativize-asset-paths.mjs; s=$?; node scripts/clean-astro-ssr-artifacts.mjs; exit $s",
    "preview": "astro preview",
    "serve": "bash scripts/serve-dashboard.sh",
    "verify": "node scripts/verify-dashboard.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.0",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^7.0.0",
    "mermaid": "^11.4.0",
    "tailwindcss": "^4.0.0",
    "zod": "^3.23.0"
  }
}
