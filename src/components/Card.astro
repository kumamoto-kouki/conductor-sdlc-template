---
// 汎用カード（.card クラス）。updated=true で左帯の強調（.card-updated）を付与。
interface Props {
  updated?: boolean;
  class?: string;
}

const { updated = false, class: className = "" } = Astro.props;
---

<div class={`card ${updated ? "card-updated" : ""} ${className}`.trim()}>
  <slot />
</div>
