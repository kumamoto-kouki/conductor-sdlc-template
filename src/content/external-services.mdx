import MermaidDiagram from "../components/MermaidDiagram.astro";

export const servicesDiagram = `flowchart LR
  CDN["Claude Design<br/>claude.ai/design<br/>デザインを生成<br/>（デザイン実施者が操作）"]
  LOCAL["ローカルリポジトリ<br/>この PC・本アプリ<br/>実装・テスト・統合<br/>コンダクター＋実施者＋レビュアー"]
  GH["GitHub<br/>公開リポジトリ<br/>コード公開・進捗管理<br/>（Projects / PR）"]
  GATE{{"🔒 人間が承認<br/>push（公開）"}}

  CDN -- "/design-sync（1部品ずつ）" --> LOCAL
  LOCAL -. 参照 .-> CDN
  LOCAL -- "push / PR" --> GH
  GH -. "clone / pull" .-> LOCAL
  GATE -.-> LOCAL

  classDef cdn fill:#eaf3fa,stroke:#2f6f9f;
  classDef local fill:#fff,stroke:#b08968,stroke-width:2px;
  classDef gh fill:#f3f1ee,stroke:#2b2622;
  classDef gate fill:#fff,stroke:#b00020,color:#b00020;
  class CDN cdn
  class LOCAL local
  class GH gh
  class GATE gate`;

<MermaidDiagram
  code={servicesDiagram}
  ariaLabel="外部サービスとの関係図。左の Claude Design、中央のローカルリポジトリ、右の GitHub がデータの流れでつながる。push（公開）は人間が承認"
  class="mx-auto max-w-[760px]"
/>

<ul class="tight mt-3">
  <li>
    <b>Claude Design（claude.ai/design）</b>：デザインを会話で生成する場所。デザイン実施者が操作し、結果を <code>/design-sync</code> で<b>1部品ずつ</b>ローカルへ取り込む。新規課金は不要だが共通の利用枠を消費。
  </li>
  <li>
    <b>ローカルリポジトリ（この PC）</b>：実装・テスト・統合の中心。コンダクター・実施者・レビュアーが動く。
  </li>
  <li>
    <b>GitHub</b>：コードを公開し進捗（Projects / PR）を管理する先。<b>push（公開）は人間が承認</b>してから。
  </li>
</ul>
